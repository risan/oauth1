# Scout: php85-toolchain

## Scope and preserved behavior
- This slice must retain every current OAuth behavior: factory wiring, request-token/callback confirmation, callback-token comparison before access-token dispatch, protected-resource token requirement, base-URI resolution, and provider presets. Production source must not change for this tooling-only slice — `src/OAuth1Factory.php:26-48`, `src/OAuth1.php:109-191`, `src/Credentials/CredentialsFactory.php:12-49`, `src/Config/UriConfig.php:206-227`, `src/Provider/*.php`.
- Preserve all present behavioral assertions while changing test syntax/runner: 28 unit-test files use PHPUnit `TestCase`, 190 `@test` metadata annotations, PHPUnit mocks, and exception assertions — `tests/Unit/`, `tests/Unit/OAuth1Test.php:33-207`, `tests/Unit/Credentials/CredentialsFactoryTest.php:57-123`.

## Current dependency and platform contract
- Runtime declares PHP `>=5.6.4`, Guzzle `~6.3`, Guzzle PSR-7 `^1.4.2`, `random_compat`, and PSR HTTP Message `^1.0`; dev requires PHPUnit `~5.7|~6.4`; no Composer scripts exist — `composer.json:24-47`.
- `random_compat` is redundant once the public platform is PHP 8.5 because the production nonce generator directly calls native `random_bytes()` — `composer.json:34-42`, `src/Request/NonceGenerator.php:30-33`.
- Guzzle/PSR-7 are direct API dependencies, not interchangeable-only transitive packages: transport constructs `GuzzleHttp\\Client` and calls `request()`, while URI code calls `Uri::isAbsolute`, `Uri::fromParts`, and `UriResolver::resolve`; tests mock `GuzzleHttp\\Client` and instantiate `GuzzleHttp\\Psr7\\Response` — `src/HttpClient.php:5-47`, `src/Request/UriParser.php:5-42`, `tests/Unit/HttpClientTest.php:6-104`.
- As checked on 2026-09-11, current Guzzle 8.2.0 requires PHP `^7.4 || ^8.0`, PSR-7 `^3.1`, and PSR HTTP Client/Factory interfaces; its published compatibility table includes PHP 8.5. Upgrade code must run the named direct URI/client calls under the resolved version rather than assuming Guzzle 6 behavior persists ([Packagist: Guzzle](https://packagist.org/packages/guzzlehttp/guzzle)).
- PHP 8.5-only constraints should be expressed as `php: ^8.5`; Composer treats PHP and extensions as platform packages, so the final manifest should declare extensions that its selected tools actually require ([Composer platform packages](https://getcomposer.org/doc/01-basic-usage.md#platform-packages)).

## Test-runner migration facts
- Legacy configuration uses removed/obsolete PHPUnit XML concepts (`backupStaticAttributes`, error/notice/warning conversion, `syntaxCheck`, filter `whitelist`, and `logging` entries) and directs all tests below `tests/`; it must be regenerated for the selected runner rather than incrementally patched — `phpunit.xml.dist:1-33`.
- Every test uses docblock `/** @test */` discovery; current PHPUnit 13 uses modern metadata and must run on PHP `>=8.4.1`. Direct PHPUnit migration therefore requires attributes/name migration plus current XML and mock APIs ([Packagist: PHPUnit](https://packagist.org/packages/phpunit/phpunit)); examples are in `tests/Unit/Request/NonceGeneratorTest.php:18-57`.
- At least 10 test files call removed legacy mock-builder `setMethods()`; they also chain obsolete builder toggles (`disableOriginalClone`, `disableArgumentCloning`, `disallowMockingUnknownTypes`). Conversion must use the selected maintained runner's supported partial-mock API while retaining the exact interaction expectations — `tests/Unit/OAuth1Test.php:295-303`, `tests/Unit/Request/RequestFactoryTest.php:39-45`, `tests/Unit/Request/ProtocolParameterTest.php:383-392`.
- `assertInternalType` and `assertRegExp` appear in nonce tests and need current equivalents (`assertIsString`, `assertMatchesRegularExpression`) if retaining PHPUnit assertions — `tests/Unit/Request/NonceGeneratorTest.php:24-57`.
- Pest is a viable choice for the requested readability upgrade: current Pest 5.1.4 requires PHP `^8.4` and PHPUnit `^13.3.2`, so it supports the required PHP 8.5 baseline. A complete conversion must preserve mock expectations and all failure cases; do not use Pest merely to reduce assertion count ([Packagist: Pest](https://packagist.org/packages/pestphp/pest)).

## Formatting, analysis, and CI
- Existing formatting is not installable from `composer.json`: `.php_cs.dist` configures the now-unrequired PHP-CS-Fixer only for `src/`; StyleCI mirrors it only for `src/`; neither formats tests — `.php_cs.dist:3-20`, `.styleci.yml:1-20`, `composer.json:41-47`.
- Current Laravel Pint 1.32.1 is a standalone PHP formatter package requiring PHP `^8.3` and is compatible with the intended platform; use a committed `pint.json` only if deviations from Pint defaults are necessary, and expose a Composer script that checks rather than mutates in CI ([Packagist: Pint](https://packagist.org/packages/laravel/pint)).
- No static analysis config or dev dependency exists. Current PHPStan 2.2.13 supports PHP `^7.4|^8.0`; add an explicit `phpstan.neon` with a deliberate initial level/path, and have the script analyze `src` plus tests only when the new type/mocking baseline makes that actionable ([Packagist: PHPStan](https://packagist.org/packages/phpstan/phpstan)).
- Travis is the sole committed CI. It tests PHP 5.6–7.2, performs `composer update` rather than install, invokes PHPUnit directly, and uploads coverage to Scrutinizer; no `.github/workflows` file is tracked — `.travis.yml:1-40`, `git ls-files '.github/*' '.github/**/*'`.
- Obsolete hosted configurations to remove with Travis are `.travis.yml`, `.styleci.yml`, `.scrutinizer.yml`, and `.php_cs.dist`; `.gitattributes` must also stop export-ignoring removed files and should retain intentional package-distribution exclusions — `.gitattributes:1-8`.

## Lockfile convention
- This reusable library currently ignores `composer.lock` and has no tracked lockfile (`.gitignore:1-4`, `git ls-files composer.lock`). The child brief explicitly assumes that convention remains unless a stronger repository decision is made.
- The CI must use `composer update` if the lockfile remains uncommitted; `composer install` is reproducible only when a lock file is present. Composer documents that `update` resolves and writes the lock file, while `install` uses it when available ([Composer basic usage](https://getcomposer.org/doc/01-basic-usage.md#updating-dependencies-to-their-latest-versions)).

## Failure-capable verification contract
- No local PHP or Composer executable is available in this scout environment (`php -v` and `composer --version` both return `command not found`), so no dependency resolution, test migration, or tool command has been run here.
- Current repository-proven test command is `vendor/bin/phpunit --no-coverage`; Travis also runs `vendor/bin/phpunit --coverage-text --coverage-clover coverage.xml` in its coverage branch — `.travis.yml:36-40`.
- Add Composer scripts with distinct failure modes and make CI invoke them after dependency resolution: `composer validate --strict`; `composer format -- --test` (Pint check-only); `composer analyse`; and `composer test`. The builder must verify their exact underlying commands after selecting PHPUnit or Pest, because no such scripts exist today — `composer.json:41-47`.
- A clean resolution check for an intentionally unlocked library is `composer update --prefer-dist --no-interaction`, followed by the four scripts. CI should fail on invalid metadata, formatting drift, static-analysis findings, and behavioral regressions independently; this is the first usable baseline for later signing/integration work.

## Implementation boundaries and open checks
- Keep `src/` byte-for-byte behavioral-equivalent in this slice. PHP typing/strict-types/public API redesign belongs only where necessary to make the chosen PHP 8.5 dependencies run, and any required source edit needs a regression test proving unchanged behavior — child brief out-of-scope rule; `src/OAuth1.php:109-191`.
- Retest `GuzzleHttp\\Psr7\\Uri`/`UriResolver` methods and the mocked `Client::request()` call against the resolved Guzzle/PSR-7 major version; these are the only direct production APIs likely affected by runtime dependency replacement — `src/Request/UriParser.php:15-42`, `src/HttpClient.php:33-47`.
- Decide once whether Pest 5 is adopted. If yes, remove direct PHPUnit as a root dev dependency because Pest owns a compatible PHPUnit 13; if no, require current PHPUnit directly. Mixing independent incompatible constraints would undermine deterministic dependency resolution — current Pest requirements and PHPUnit requirements are published on [Pest](https://packagist.org/packages/pestphp/pest) and [PHPUnit](https://packagist.org/packages/phpunit/phpunit).
